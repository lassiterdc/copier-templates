version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "[[python_version]]"

mkdocs:
  configuration: mkdocs.yml

python:
  install:
    - method: pip
      path: .
    - requirements: requirements.txt
